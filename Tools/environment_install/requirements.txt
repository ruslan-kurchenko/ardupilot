# ArduPilot Development Environment Requirements
# This file ensures compatibility with MAVProxy and SITL tools
# 
# CRITICAL: NumPy must be <2.0 due to MAVProxy compatibility issues
# MAVProxy uses deprecated np.fromstring() which was removed in NumPy 2.0
numpy<2.0

# Core ArduPilot packages
pymavlink>=2.4.40
MAVProxy>=1.8.70

# Essential dependencies
future>=0.18.0
lxml>=4.9.0
pyserial>=3.5
empy==3.3.4
ptyprocess>=0.7.0
dronecan>=1.0.20
geocoder>=1.38.0
pynmeagps>=1.0.0

# Development and testing tools
flake8>=6.0.0
junitparser>=3.0.0
wsproto>=1.2.0
tabulate>=0.9.0

# Graphics and GUI packages (conditional - see install script)
# These are installed separately based on SKIP_AP_GRAPHIC_ENV
matplotlib>=3.6.0
scipy>=1.9.0
opencv-python>=4.8.0,<5.0
wxpython>=4.2.0
attrdict3>=2.0.0
pyyaml>=6.0

# Game development and utilities (conditional - see install script)
# These are installed separately based on SKIP_AP_EXT_ENV
pygame>=2.1.0
intelhex>=2.3.0

# System utilities
psutil>=5.9.0
pyparsing>=3.0.0